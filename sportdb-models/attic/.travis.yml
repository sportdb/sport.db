##############################
# Travis CI Ruby Settings
#   more info see -> about.travis-ci.org/docs/user/languages/ruby
#
#
# fix: Please note that you need to add rake to your Gemfile
# (adding it to just :test group should be sufficient).
#
# fix: add travis build status image to readme


#
# note: use
#  $ rake debug_gem > sportdb.gemspec
#  to update gemspec w/ rake task 


language: ruby
rvm:
  - "2.1.0"
  - "2.0.0"
  - "1.9.3"
  - "1.9.2"
 
#  check what to do w/ sqlite3  - needs native extension?? will fail on bundle install
# - jruby-19mode    # JRuby in 1.9 mode

